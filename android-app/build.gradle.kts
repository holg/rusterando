// Root build script. The Android Gradle Plugin + Kotlin plugin versions
// here have to stay in sync; bump them together.
plugins {
    id("com.android.application") version "8.5.2" apply false
    id("org.jetbrains.kotlin.android") version "1.9.24" apply false
}
