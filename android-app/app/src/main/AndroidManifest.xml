<?xml version="1.0" encoding="utf-8"?>
<!--
    Single-Activity WebView shell. The applicationId, app_name string,
    and BuildConfig.START_URL come from the per-flavor block in
    build.gradle.kts so this manifest stays shop-neutral.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="false"
        android:theme="@style/Theme.Rusterando">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
                Deep links into the configured shop. The actual host is
                injected via @string/associated_domain which each flavor
                redefines under app/src/<flavor>/res/values/strings.xml.
                autoVerify enables App Links once the AASA-equivalent
                .well-known/assetlinks.json is served on that host.
            -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" />
                <data android:host="@string/associated_domain" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ShopsActivity"
            android:exported="false"
            android:label="Shops"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".ShopDetailActivity"
            android:exported="false"
            android:label="Shop"
            android:parentActivityName=".ShopsActivity" />
    </application>
</manifest>
